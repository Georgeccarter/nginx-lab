# Run the following:
# Refreshes Kali’s package index to ensure you download the latest available versions of all software, including Nginx.
sudo apt update
# Downloads and installs the Nginx web server along with all required dependencies. The -y flag auto-confirms the installation.
sudo apt install -y nginx
# Launches the Nginx service so it begins serving web content immediately.
sudo systemctl start nginx
# Configures Nginx to automatically start whenever Kali boots, ensuring your web server is always available.
sudo systemctl enable nginx
# Displays whether Nginx is running, along with log messages and uptime. This confirms the service started successfully.
sudo systemctl status nginx
# Sends a request to the Nginx server and displays the response headers. A 200 OK indicates the server is functioning correctly
curl -I http://localhost
# Loads the default Nginx web page, confirming that the server is reachable via HTTP.
http://localhost
